[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "localaiutils"
version = "0.1.0"
description = "Local inference configuration helpers"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/localaiutils"]
